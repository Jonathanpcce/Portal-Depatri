rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Toda leitura e gravação passa pelas funções com sessão e perfil verificados.
    match /{document=**} { allow read, write: if false; }
  }
}
